import React from 'react';
import { NextPage } from 'next';

import NotFound from 'src/containers/not-found/NotFound';

const NotFoundPage: NextPage = () => <NotFound />;

export default NotFoundPage;
